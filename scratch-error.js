import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('pageerror', err => console.log('PAGE_ERROR:', err.toString())); page.on('console', msg => { if (msg.type() === 'error') { console.log('CONSOLE_ERROR:', msg.text()); for (let i = 0; i < msg.args().length; i++) console.log(msg.args()[i].toString()); } }); await page.goto('http://localhost:3000/clinical-notes/assessments', { waitUntil: 'networkidle0' }); await new Promise(r => setTimeout(r, 2000)); await browser.close(); console.log('Done.'); })();
